jdk:
  - openjdk17
before_install:
  - sdk install java 17.0.3-tem
  - sdk use java 17.0.3-tem
install:
  - echo "Running a custom install command: 'assemble' -> 'core:build'"
  - ./gradlew clean -Pgroup=$GROUP -Pversion=$VERSION -xtest core:build publishToMavenLocal
